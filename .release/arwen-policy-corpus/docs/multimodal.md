# Multimodal Processing

The architecture supports:

- HTML;
- PDF;
- DOCX;
- text;
- scanned documents;
- audio;
- video.

Optional adapters may provide OCR, ASR and speaker diarization.

Original artifacts must remain separate from derived transcripts and extracted text. Every derived representation should identify its processing method and preserve timestamps or page references when available.
