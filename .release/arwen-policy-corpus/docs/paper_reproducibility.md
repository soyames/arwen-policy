# Paper Reproducibility

Every reported experiment should record:

- Git commit SHA;
- configuration revision;
- Hugging Face dataset revision(s);
- benchmark version;
- model identifier and revision;
- evaluation results;
- environment/dependency lock information.

The `arwen_paper` package provides a small manifest helper for this purpose.
